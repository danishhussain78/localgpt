# --- Core Django ---
Django>=4.2

# --- Database and ORM support (optional, if using Postgres/MySQL, add their driver below) ---
# psycopg2-binary>=2.9.9   # Uncomment for PostgreSQL
# mysqlclient>=2.2.0       # Uncomment for MySQL

# --- File handling ---
PyPDF2>=3.0.1
python-docx>=1.0.0

# --- NLP / Embedding ---
sentence-transformers>=3.0.0
chromadb>=0.5.5

# --- HTTP requests ---
requests>=2.32.3

# --- JSON / Utility ---
# (json and os are built-in; no need to include them)

# --- Optional: For deployment & storage backends ---
# gunicorn>=21.2.0        # if deploying on Linux servers
# django-storages>=1.14.2 # if using S3 or other cloud storage
